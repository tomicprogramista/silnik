"""
This part is example-specific.
Normally you won't need this in your code.

It sets up the python path to include the root folder of the library.
"""
import sys
import os
sys.path.append(os.path.join(sys.path[0], '..'))